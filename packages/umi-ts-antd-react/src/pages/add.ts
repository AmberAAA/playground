/**
 * @param a
 * @param b
 * @returns
 */
export const add = (a: number, b: number) => {
  return a + b;
};

/**
 * 
 * @param a 
 * @param b 
 * @returns 
 */
export const sub = (a: number, b: number) => a - b;